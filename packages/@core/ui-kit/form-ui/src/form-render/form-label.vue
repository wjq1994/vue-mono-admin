<script setup lang="ts">
import { FormLabel, SdHelpTooltip } from '@sd-core/shadcn-ui';
import { cn } from '@sd-core/shared/utils';

interface Props {
  class?: string;
  help?: string;
  required?: boolean;
}

const props = defineProps<Props>();
</script>

<template>
  <FormLabel :class="cn('flex items-center', props.class)">
    <span v-if="required" class="text-destructive mr-[2px]">*</span>
    <slot></slot>
    <SdHelpTooltip v-if="help" trigger-class="size-3.5 ml-1">
      {{ help }}
    </SdHelpTooltip>
  </FormLabel>
</template>
